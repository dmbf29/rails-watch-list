<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, collection: Movie.where.not(id: @list.movies).order(:title), include_blank: false %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>